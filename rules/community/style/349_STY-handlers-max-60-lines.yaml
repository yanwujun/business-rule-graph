name: "STY-handlers-max-60-lines"
description: "Functions in handlers/ should be <= 60 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "handlers/**"
  require:
    max_symbol_lines: 60
