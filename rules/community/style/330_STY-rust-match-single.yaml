name: STY-rust-match-single
description: "Single-arm match; use if let instead."
severity: info
type: ast_match
match:
  ast: "match $EXPR { $PATTERN => $BODY }"
  language: rust
  file_glob: "**/*.rs"
