name: STY-ts-class-naming
description: "TypeScript class names should use PascalCase."
severity: info
type: symbol_match
match:
  kind: class
  file_glob: "**/*.ts"
  require:
    name_regex: "^[a-z]"
