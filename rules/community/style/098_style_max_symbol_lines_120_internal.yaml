name: "Style: max symbol lines 120 (internal)"
description: "Functions and methods in internal should be <= 120 lines."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "internal/**"
  require:
    max_symbol_lines: 120
