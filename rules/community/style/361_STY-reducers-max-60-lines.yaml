name: "STY-reducers-max-60-lines"
description: "Functions in reducers/ should be <= 60 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "reducers/**"
  require:
    max_symbol_lines: 60
