name: "Style: style constants upper case js"
description: "JavaScript constants should use UPPER_SNAKE_CASE."
severity: info
type: symbol_match

match:
  kind: [variable]
  file_glob: "**/*.js"
  require:
    name_regex: "^[A-Z][A-Z0-9_]*$"
