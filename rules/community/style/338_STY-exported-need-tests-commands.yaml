name: "STY-exported-need-tests-commands"
description: "Exported functions in commands/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "commands/**"
  exported: true
  require:
    has_test: true
