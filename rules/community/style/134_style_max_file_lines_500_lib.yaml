name: "Style: max file lines 500 (lib)"
description: "Critical files should stay below 500 lines."
severity: warning
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "lib/**"
  require:
    max_file_lines: 500
