name: "Style: max symbol lines 120 (domain)"
description: "Functions and methods in domain should be <= 120 lines."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "domain/**"
  require:
    max_symbol_lines: 120
