name: STY-php-nested-ternary
description: "Nested ternary is confusing; use if/elseif."
severity: warning
type: ast_match
match:
  ast: "$A ? $B : $C ? $D : $E"
  language: php
  file_glob: "**/*.php"
