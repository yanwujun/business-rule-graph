name: STY-py-constant-naming
description: "Python constants should use UPPER_SNAKE_CASE."
severity: info
type: symbol_match
match:
  kind: function
  file_glob: "**/*.py"
  require:
    name_regex: "^[a-z].*_[a-z]"
