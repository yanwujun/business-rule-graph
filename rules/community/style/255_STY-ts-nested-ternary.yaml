name: STY-ts-nested-ternary
description: "Nested ternary is hard to read; use if/else."
severity: info
type: ast_match
match:
  ast: "$A ? $B : $C ? $D : $E"
  language: typescript
  file_glob: "**/*.ts"
