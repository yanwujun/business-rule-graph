name: "Style: max symbol lines 120 (handlers)"
description: "Functions and methods in handlers should be <= 120 lines."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "handlers/**"
  require:
    max_symbol_lines: 120
