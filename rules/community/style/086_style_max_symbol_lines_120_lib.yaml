name: "Style: max symbol lines 120 (library)"
description: "Functions and methods in library should be <= 120 lines."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "lib/**"
  require:
    max_symbol_lines: 120
