name: "Style: max symbol lines 120 (package)"
description: "Functions and methods in package should be <= 120 lines."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "pkg/**"
  require:
    max_symbol_lines: 120
