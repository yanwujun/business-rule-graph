name: STY-go-exported-naming
description: "Go exported names should use PascalCase."
severity: info
type: symbol_match
match:
  kind: function
  file_glob: "**/*.go"
  require:
    name_regex: "^[A-Z].*_"
