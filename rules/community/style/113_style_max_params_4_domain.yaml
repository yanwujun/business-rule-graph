name: "Style: max params 4 (domain)"
description: "High-risk directories should keep parameter counts low."
severity: warning
type: symbol_match

match:
  kind: [function, method]
  file_glob: "domain/**"
  require:
    max_params: 4
