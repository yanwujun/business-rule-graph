name: "STY-exported-need-tests-models"
description: "Exported functions in models/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "models/**"
  exported: true
  require:
    has_test: true
