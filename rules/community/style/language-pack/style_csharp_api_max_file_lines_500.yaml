name: "Style: csharp api max file lines 500"
description: "Csharp files in api/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "api/**/*.cs"
  require:
    max_file_lines: 500
