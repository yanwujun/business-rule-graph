name: "Style: go app max file lines 500"
description: "Go files in app/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "app/**/*.go"
  require:
    max_file_lines: 500
