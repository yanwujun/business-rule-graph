name: "Style: kotlin services max params 4"
description: "Kotlin functions/methods in services/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "services/**/*.kt"
  require:
    max_params: 4
