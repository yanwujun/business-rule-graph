name: "Style: csharp internal max file lines 500"
description: "Csharp files in internal/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "internal/**/*.cs"
  require:
    max_file_lines: 500
