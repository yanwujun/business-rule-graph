name: "Style: rust app max params 4"
description: "Rust functions/methods in app/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "app/**/*.rs"
  require:
    max_params: 4
