name: "Style: swift services max params 6"
description: "Swift functions/methods in services/ should use at most 6 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "services/**/*.swift"
  require:
    max_params: 6
