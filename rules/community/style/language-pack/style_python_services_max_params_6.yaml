name: "Style: python services max params 6"
description: "Python functions/methods in services/ should use at most 6 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "services/**/*.py"
  require:
    max_params: 6
