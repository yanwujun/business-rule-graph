name: "Style: go src max params 6"
description: "Go functions/methods in src/ should use at most 6 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "src/**/*.go"
  require:
    max_params: 6
