name: "Style: rust internal max file lines 500"
description: "Rust files in internal/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "internal/**/*.rs"
  require:
    max_file_lines: 500
