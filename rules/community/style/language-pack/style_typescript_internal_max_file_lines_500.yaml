name: "Style: typescript internal max file lines 500"
description: "Typescript files in internal/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "internal/**/*.ts"
  require:
    max_file_lines: 500
