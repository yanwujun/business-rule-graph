name: "Style: kotlin app max params 4"
description: "Kotlin functions/methods in app/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "app/**/*.kt"
  require:
    max_params: 4
