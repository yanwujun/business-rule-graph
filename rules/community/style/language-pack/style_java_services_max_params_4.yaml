name: "Style: java services max params 4"
description: "Java functions/methods in services/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "services/**/*.java"
  require:
    max_params: 4
