name: "Style: ruby api max file lines 500"
description: "Ruby files in api/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "api/**/*.rb"
  require:
    max_file_lines: 500
