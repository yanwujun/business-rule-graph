name: "Style: swift app max file lines 500"
description: "Swift files in app/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "app/**/*.swift"
  require:
    max_file_lines: 500
