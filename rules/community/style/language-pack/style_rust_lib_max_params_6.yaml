name: "Style: rust lib max params 6"
description: "Rust functions/methods in lib/ should use at most 6 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "lib/**/*.rs"
  require:
    max_params: 6
