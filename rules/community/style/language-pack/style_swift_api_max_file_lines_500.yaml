name: "Style: swift api max file lines 500"
description: "Swift files in api/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "api/**/*.swift"
  require:
    max_file_lines: 500
