name: "Style: rust src max file lines 500"
description: "Rust files in src/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "src/**/*.rs"
  require:
    max_file_lines: 500
