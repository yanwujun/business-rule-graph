name: "Style: swift src max params 4"
description: "Swift functions/methods in src/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "src/**/*.swift"
  require:
    max_params: 4
