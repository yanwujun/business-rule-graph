name: "Style: swift services max file lines 500"
description: "Swift files in services/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "services/**/*.swift"
  require:
    max_file_lines: 500
