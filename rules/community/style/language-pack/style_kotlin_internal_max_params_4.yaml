name: "Style: kotlin internal max params 4"
description: "Kotlin functions/methods in internal/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "internal/**/*.kt"
  require:
    max_params: 4
