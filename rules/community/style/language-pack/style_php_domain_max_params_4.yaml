name: "Style: php domain max params 4"
description: "Php functions/methods in domain/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "domain/**/*.php"
  require:
    max_params: 4
