name: "Style: java domain max file lines 500"
description: "Java files in domain/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "domain/**/*.java"
  require:
    max_file_lines: 500
