name: "Style: rust src max params 6"
description: "Rust functions/methods in src/ should use at most 6 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "src/**/*.rs"
  require:
    max_params: 6
