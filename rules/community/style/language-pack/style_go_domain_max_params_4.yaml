name: "Style: go domain max params 4"
description: "Go functions/methods in domain/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "domain/**/*.go"
  require:
    max_params: 4
