name: "Style: typescript lib max file lines 500"
description: "Typescript files in lib/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "lib/**/*.ts"
  require:
    max_file_lines: 500
