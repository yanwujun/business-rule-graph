name: "Style: ruby internal max file lines 500"
description: "Ruby files in internal/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "internal/**/*.rb"
  require:
    max_file_lines: 500
