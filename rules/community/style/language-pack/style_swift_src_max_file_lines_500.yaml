name: "Style: swift src max file lines 500"
description: "Swift files in src/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "src/**/*.swift"
  require:
    max_file_lines: 500
