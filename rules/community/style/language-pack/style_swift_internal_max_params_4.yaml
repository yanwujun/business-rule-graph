name: "Style: swift internal max params 4"
description: "Swift functions/methods in internal/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "internal/**/*.swift"
  require:
    max_params: 4
