name: "Style: java src max file lines 500"
description: "Java files in src/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "src/**/*.java"
  require:
    max_file_lines: 500
