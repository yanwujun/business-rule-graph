name: "Style: ruby services max file lines 500"
description: "Ruby files in services/ should stay under 500 lines."
severity: info
type: symbol_match

match:
  kind: [function, method, class]
  file_glob: "services/**/*.rb"
  require:
    max_file_lines: 500
