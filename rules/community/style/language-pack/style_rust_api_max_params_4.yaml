name: "Style: rust api max params 4"
description: "Rust functions/methods in api/ should use at most 4 parameters."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "api/**/*.rs"
  require:
    max_params: 4
