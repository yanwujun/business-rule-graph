name: "STY-exported-need-tests-filters"
description: "Exported functions in filters/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "filters/**"
  exported: true
  require:
    has_test: true
