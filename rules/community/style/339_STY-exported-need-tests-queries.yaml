name: "STY-exported-need-tests-queries"
description: "Exported functions in queries/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "queries/**"
  exported: true
  require:
    has_test: true
