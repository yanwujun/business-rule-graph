name: "STY-actions-max-100-lines"
description: "Functions in actions/ should be <= 100 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "actions/**"
  require:
    max_symbol_lines: 100
