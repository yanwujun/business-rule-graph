name: STY-js-magic-number
description: "Magic number in condition; extract to named constant."
severity: info
type: ast_match
match:
  ast: "if ($X === $NUM)"
  language: javascript
  file_glob: "**/*.js"
