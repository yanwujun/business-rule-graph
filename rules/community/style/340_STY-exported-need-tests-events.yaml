name: "STY-exported-need-tests-events"
description: "Exported functions in events/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "events/**"
  exported: true
  require:
    has_test: true
