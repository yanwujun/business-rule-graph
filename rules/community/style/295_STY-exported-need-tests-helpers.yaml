name: "STY-exported-need-tests-helpers"
description: "Exported functions in helpers/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "helpers/**"
  exported: true
  require:
    has_test: true
