name: "STY-selectors-max-100-lines"
description: "Functions in selectors/ should be <= 100 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "selectors/**"
  require:
    max_symbol_lines: 100
