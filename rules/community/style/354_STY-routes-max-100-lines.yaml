name: "STY-routes-max-100-lines"
description: "Functions in routes/ should be <= 100 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "routes/**"
  require:
    max_symbol_lines: 100
