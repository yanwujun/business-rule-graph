name: "STY-exported-need-tests-repositories"
description: "Exported functions in repositories/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "repositories/**"
  exported: true
  require:
    has_test: true
