name: STY-js-test-expect-eq
description: "toBe uses Object.is; use toEqual for deep comparison."
severity: info
type: ast_match
match:
  ast: "expect($X).toBe($Y)"
  language: javascript
  file_glob: "**/*.js"
