name: STY-go-unexported-naming
description: "Go unexported names should use camelCase."
severity: info
type: symbol_match
match:
  kind: function
  file_glob: "**/*.go"
  require:
    name_regex: "^[a-z].*_[a-z]"
