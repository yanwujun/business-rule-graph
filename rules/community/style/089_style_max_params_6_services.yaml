name: "Style: max params 6 (services)"
description: "Functions and methods in services should have <= 6 parameters."
severity: warning
type: symbol_match

match:
  kind: [function, method]
  file_glob: "services/**"
  require:
    max_params: 6
