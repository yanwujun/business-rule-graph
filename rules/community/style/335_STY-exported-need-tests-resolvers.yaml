name: "STY-exported-need-tests-resolvers"
description: "Exported functions in resolvers/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "resolvers/**"
  exported: true
  require:
    has_test: true
