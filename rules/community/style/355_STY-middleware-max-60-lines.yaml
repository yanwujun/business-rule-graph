name: "STY-middleware-max-60-lines"
description: "Functions in middleware/ should be <= 60 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "middleware/**"
  require:
    max_symbol_lines: 60
