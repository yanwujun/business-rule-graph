name: "STY-exported-need-tests-middleware"
description: "Exported functions in middleware/ should have test coverage."
severity: warning
type: symbol_match
match:
  kind: [function, class]
  file_glob: "middleware/**"
  exported: true
  require:
    has_test: true
