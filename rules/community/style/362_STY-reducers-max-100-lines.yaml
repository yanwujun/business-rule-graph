name: "STY-reducers-max-100-lines"
description: "Functions in reducers/ should be <= 100 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "reducers/**"
  require:
    max_symbol_lines: 100
