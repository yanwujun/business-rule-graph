name: STY-cs-boolean-return
description: "Simplify to return cond."
severity: info
type: ast_match
match:
  ast: "if ($COND) { return true; } else { return false; }"
  language: csharp
  file_glob: "**/*.cs"
