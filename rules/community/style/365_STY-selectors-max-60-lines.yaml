name: "STY-selectors-max-60-lines"
description: "Functions in selectors/ should be <= 60 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "selectors/**"
  require:
    max_symbol_lines: 60
