name: "Style: max symbol lines 120 (api layer)"
description: "Functions and methods in api layer should be <= 120 lines."
severity: info
type: symbol_match

match:
  kind: [function, method]
  file_glob: "api/**"
  require:
    max_symbol_lines: 120
