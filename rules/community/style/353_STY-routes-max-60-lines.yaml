name: "STY-routes-max-60-lines"
description: "Functions in routes/ should be <= 60 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "routes/**"
  require:
    max_symbol_lines: 60
