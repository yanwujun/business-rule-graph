name: STY-js-func-naming
description: "JavaScript function names should use camelCase."
severity: info
type: symbol_match
match:
  kind: function
  file_glob: "**/*.js"
  require:
    name_regex: "^[A-Z]"
