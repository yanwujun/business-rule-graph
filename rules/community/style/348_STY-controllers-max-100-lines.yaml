name: "STY-controllers-max-100-lines"
description: "Functions in controllers/ should be <= 100 lines."
severity: info
type: symbol_match
match:
  kind: [function, method]
  file_glob: "controllers/**"
  require:
    max_symbol_lines: 100
