name: COR-350-py-pathlib-open
description: "Use with statement for Path.open() to ensure cleanup."
severity: info
type: ast_match
match:
  ast: "Path($PATH).open()"
  language: python
  file_glob: "**/*.py"
