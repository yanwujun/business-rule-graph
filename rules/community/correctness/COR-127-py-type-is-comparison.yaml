name: COR-127-py-type-is-comparison
description: "Use isinstance() instead of type() is comparison."
severity: warning
type: ast_match
match:
  ast: "type($X) is $TYPE"
  language: python
  file_glob: "**/*.py"
