name: COR-489-py-tempfile-cleanup
description: "Temp directory not auto-cleaned; use TemporaryDirectory."
severity: info
type: ast_match
match:
  ast: "tempfile.mkdtemp()"
  language: python
  file_glob: "**/*.py"
