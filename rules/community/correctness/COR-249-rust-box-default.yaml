name: COR-249-rust-box-default
description: "Use Box::default() instead of Box::new(T::default())."
severity: info
type: ast_match
match:
  ast: "Box::new($TYPE::default())"
  language: rust
  file_glob: "**/*.rs"
