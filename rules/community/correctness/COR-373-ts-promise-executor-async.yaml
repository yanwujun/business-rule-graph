name: COR-373-ts-promise-executor-async
description: "Async promise executor is an anti-pattern."
severity: error
type: ast_match
match:
  ast: "new Promise(async ($RESOLVE, $REJECT) => $BODY)"
  language: typescript
  file_glob: "**/*.ts"
