name: COR-138-py-datetime-now
description: "datetime.now() uses local time; prefer datetime.utcnow() or timezone-aware."
severity: info
type: ast_match
match:
  ast: "datetime.now()"
  language: python
  file_glob: "**/*.py"
