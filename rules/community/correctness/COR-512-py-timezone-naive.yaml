name: COR-512-py-timezone-naive
description: "datetime.now() is timezone-naive; use datetime.now(tz=UTC)."
severity: info
type: ast_match
match:
  ast: "datetime.datetime.now()"
  language: python
  file_glob: "**/*.py"
