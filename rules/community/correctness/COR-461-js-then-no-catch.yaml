name: COR-461-js-then-no-catch
description: "Promise .then without .catch may silently fail."
severity: warning
type: ast_match
match:
  ast: ".then($SUCCESS)"
  language: javascript
  file_glob: "**/*.js"
