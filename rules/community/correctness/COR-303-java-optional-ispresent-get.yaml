name: COR-303-java-optional-ispresent-get
description: "Use Optional.map() or ifPresent() instead of isPresent+get."
severity: info
type: ast_match
match:
  ast: "if ($OPT.isPresent()) { $OPT.get() }"
  language: java
  file_glob: "**/*.java"
