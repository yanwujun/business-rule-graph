name: COR-313-py-string-startswith-tuple
description: "Use startswith((x, y)) instead of chained startswith."
severity: info
type: ast_match
match:
  ast: "$S.startswith($X) or $S.startswith($Y)"
  language: python
  file_glob: "**/*.py"
