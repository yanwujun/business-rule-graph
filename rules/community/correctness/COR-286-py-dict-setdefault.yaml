name: COR-286-py-dict-setdefault
description: "Use dict.setdefault() or defaultdict."
severity: info
type: ast_match
match:
  ast: "if $KEY not in $DICT: $DICT[$KEY] = $VALUE"
  language: python
  file_glob: "**/*.py"
