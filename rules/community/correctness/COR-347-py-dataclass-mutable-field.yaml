name: COR-347-py-dataclass-mutable-field
description: "Mutable default in dataclass field; use default_factory."
severity: error
type: ast_match
match:
  ast: "field(default=[])"
  language: python
  file_glob: "**/*.py"
