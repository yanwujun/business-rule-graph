name: COR-415-rust-option-map-unwrap
description: "map().unwrap() on Option; use map_or or match."
severity: info
type: ast_match
match:
  ast: "$OPT.map($F).unwrap()"
  language: rust
  file_glob: "**/*.rs"
