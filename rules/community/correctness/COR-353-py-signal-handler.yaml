name: COR-353-py-signal-handler
description: "Signal handlers have restrictions; keep them minimal."
severity: info
type: ast_match
match:
  ast: "signal.signal($SIG, $HANDLER)"
  language: python
  file_glob: "**/*.py"
