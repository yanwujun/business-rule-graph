name: py-except-pass-logging
description: "Exception caught without logging hides failures."
severity: warning
type: ast_match
match:
  ast: "except $EXCEPTION as $E: pass"
  language: python
  file_glob: "**/*.py"
