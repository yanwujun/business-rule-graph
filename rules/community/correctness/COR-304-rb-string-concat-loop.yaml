name: COR-304-rb-string-concat-loop
description: "String concatenation in loop; use join or StringIO."
severity: info
type: ast_match
match:
  ast: "$S += $PART"
  language: ruby
  file_glob: "**/*.rb"
