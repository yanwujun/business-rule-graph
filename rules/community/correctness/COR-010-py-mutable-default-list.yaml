name: py-mutable-default-list
description: "Mutable default list argument is shared across calls."
severity: error
type: ast_match
match:
  ast: "def $FUNC($ARG=[])"
  language: python
  file_glob: "**/*.py"
