name: COR-158-js-confirm
description: "confirm() in production code; use proper UI dialog."
severity: warning
type: ast_match
match:
  ast: "confirm($MSG)"
  language: javascript
  file_glob: "**/*.js"
