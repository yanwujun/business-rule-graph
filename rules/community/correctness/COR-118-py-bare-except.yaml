name: COR-118-py-bare-except
description: "Bare except catches all exceptions including SystemExit."
severity: warning
type: ast_match
match:
  ast: "except:"
  language: python
  file_glob: "**/*.py"
