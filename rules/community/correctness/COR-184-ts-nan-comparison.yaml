name: COR-184-ts-nan-comparison
description: "NaN === NaN is always false; use Number.isNaN()."
severity: error
type: ast_match
match:
  ast: "$X === NaN"
  language: typescript
  file_glob: "**/*.ts"
