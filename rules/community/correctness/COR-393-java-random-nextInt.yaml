name: COR-393-java-random-nextInt
description: "Creating new Random each time reduces randomness."
severity: warning
type: ast_match
match:
  ast: "new Random().nextInt()"
  language: java
  file_glob: "**/*.java"
