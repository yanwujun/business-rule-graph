name: COR-526-py-chardet-detect
description: "Character detection is heuristic; validate results."
severity: info
type: ast_match
match:
  ast: "chardet.detect($DATA)"
  language: python
  file_glob: "**/*.py"
