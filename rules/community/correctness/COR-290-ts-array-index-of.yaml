name: COR-290-ts-array-index-of
description: "Use Array.includes() instead of indexOf !== -1."
severity: info
type: ast_match
match:
  ast: "$ARR.indexOf($ELEM) !== -1"
  language: typescript
  file_glob: "**/*.ts"
