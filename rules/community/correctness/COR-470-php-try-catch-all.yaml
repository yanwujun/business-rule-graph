name: COR-470-php-try-catch-all
description: "Empty catch block; at minimum log the error."
severity: warning
type: ast_match
match:
  ast: "catch (Exception $E) { }"
  language: php
  file_glob: "**/*.php"
