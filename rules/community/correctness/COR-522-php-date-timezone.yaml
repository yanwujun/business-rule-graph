name: COR-522-php-date-timezone
description: "date() uses server timezone; specify timezone explicitly."
severity: info
type: ast_match
match:
  ast: "date($FORMAT)"
  language: php
  file_glob: "**/*.php"
