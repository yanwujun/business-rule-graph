name: COR-292-ts-object-keys-length
description: "Use dedicated isEmpty helper for clarity."
severity: info
type: ast_match
match:
  ast: "Object.keys($OBJ).length === 0"
  language: typescript
  file_glob: "**/*.ts"
