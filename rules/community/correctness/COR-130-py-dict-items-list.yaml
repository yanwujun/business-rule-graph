name: COR-130-py-dict-items-list
description: "dict.items() already iterable in Python 3; list() is unnecessary."
severity: info
type: ast_match
match:
  ast: "list($DICT.items())"
  language: python
  file_glob: "**/*.py"
