name: COR-165-js-nan-comparison-eq
description: "NaN comparison always false; use Number.isNaN()."
severity: error
type: ast_match
match:
  ast: "$X == NaN"
  language: javascript
  file_glob: "**/*.js"
