name: COR-174-js-with-statement
description: "with statement is confusing and forbidden in strict mode."
severity: error
type: ast_match
match:
  ast: "with ($OBJ) { $BODY }"
  language: javascript
  file_glob: "**/*.js"
