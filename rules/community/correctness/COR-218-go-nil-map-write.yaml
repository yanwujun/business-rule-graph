name: COR-218-go-nil-map-write
description: "Writing to nil map causes panic; initialize with make()."
severity: error
type: ast_match
match:
  ast: "$MAP[$KEY] = $VALUE"
  language: go
  file_glob: "**/*.go"
