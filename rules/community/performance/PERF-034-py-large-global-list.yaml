name: py-large-global-list
description: "Large list at module level wastes memory if rarely used."
severity: info
type: ast_match
match:
  ast: "$GLOBAL = [$ITEMS]"
  language: python
  file_glob: "**/*.py"
