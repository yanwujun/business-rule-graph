name: go-string-concat-loop
description: "String concatenation in loop is O(n^2). Use strings.Builder."
severity: warning
type: ast_match
match:
  ast: "$S += $PART"
  language: go
  file_glob: "**/*.go"
