name: PERF-93-js-queryselector-loop
description: "DOM query in loop; cache the result."
severity: warning
type: ast_match
match:
  ast: "document.querySelector($SEL)"
  language: javascript
  file_glob: "**/*.js"
