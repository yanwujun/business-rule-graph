name: PERF-109-js-date-now-loop
description: "Date.now() in tight loop; cache if precision allows."
severity: info
type: ast_match
match:
  ast: "Date.now()"
  language: javascript
  file_glob: "**/*.js"
