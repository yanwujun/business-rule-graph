name: PERF-231-js-textencoder-loop
description: "TextEncoder creation in loop; reuse instance."
severity: info
type: ast_match
match:
  ast: "new TextEncoder()"
  language: javascript
  file_glob: "**/*.js"
