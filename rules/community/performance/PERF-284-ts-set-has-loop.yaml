name: PERF-284-ts-set-has-loop
description: "Set.has is O(1); good for membership testing."
severity: info
type: ast_match
match:
  ast: "$SET.has($ELEM)"
  language: typescript
  file_glob: "**/*.ts"
