name: PERF-67-py-list-in-check
description: "Use set literal for membership test instead of list."
severity: info
type: ast_match
match:
  ast: "$X in [$ITEMS]"
  language: python
  file_glob: "**/*.py"
