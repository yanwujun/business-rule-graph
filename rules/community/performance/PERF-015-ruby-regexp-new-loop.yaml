name: ruby-regexp-new-loop
description: "Regexp.new inside loop recompiles on every iteration."
severity: warning
type: ast_match
match:
  ast: "Regexp.new($PATTERN)"
  language: ruby
  file_glob: "**/*.rb"
