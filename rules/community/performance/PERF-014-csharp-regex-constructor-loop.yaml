name: csharp-regex-constructor-loop
description: "new Regex() inside loop recompiles on every iteration."
severity: warning
type: ast_match
match:
  ast: "new Regex($PATTERN)"
  language: c_sharp
  file_glob: "**/*.cs"
