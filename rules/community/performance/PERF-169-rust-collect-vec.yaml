name: PERF-169-rust-collect-vec
description: "collect into Vec; verify iterator is not re-iterable."
severity: info
type: ast_match
match:
  ast: ".collect::<Vec<$T>>()"
  language: rust
  file_glob: "**/*.rs"
