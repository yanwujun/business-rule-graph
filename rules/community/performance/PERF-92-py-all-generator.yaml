name: PERF-92-py-all-generator
description: "all() accepts generator; remove list wrapper for early exit."
severity: info
type: ast_match
match:
  ast: "all([$ITEMS])"
  language: python
  file_glob: "**/*.py"
