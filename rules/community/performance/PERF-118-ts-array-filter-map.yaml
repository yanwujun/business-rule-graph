name: PERF-118-ts-array-filter-map
description: "filter().map() iterates twice; use reduce for single pass."
severity: info
type: ast_match
match:
  ast: "$ARR.filter($F1).map($F2)"
  language: typescript
  file_glob: "**/*.ts"
