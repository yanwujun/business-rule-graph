name: rust-diesel-find-loop
description: "Diesel find inside loop causes N+1 problem."
severity: warning
type: ast_match
match:
  ast: "$TABLE.find($ID).first($CONN)"
  language: rust
  file_glob: "**/*.rs"
