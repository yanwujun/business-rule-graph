name: PERF-120-ts-regex-literal-loop
description: "Regex construction in loop; compile once outside loop."
severity: warning
type: ast_match
match:
  ast: "new RegExp($PATTERN)"
  language: typescript
  file_glob: "**/*.ts"
