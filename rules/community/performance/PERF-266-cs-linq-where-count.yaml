name: PERF-266-cs-linq-where-count
description: "Use Count with predicate instead of Where.Count."
severity: info
type: ast_match
match:
  ast: "$SEQ.Where($PRED).Count()"
  language: csharp
  file_glob: "**/*.cs"
