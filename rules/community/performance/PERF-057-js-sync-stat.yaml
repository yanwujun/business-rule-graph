name: js-sync-stat
description: "statSync blocks the event loop in async context."
severity: warning
type: ast_match
match:
  ast: "fs.statSync($PATH)"
  language: javascript
  file_glob: "**/*.{js,mjs}"
