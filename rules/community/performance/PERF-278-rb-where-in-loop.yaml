name: PERF-278-rb-where-in-loop
description: "ActiveRecord query in loop; batch with where(id: ids)."
severity: warning
type: ast_match
match:
  ast: "$MODEL.where($COND)"
  language: ruby
  file_glob: "**/*.rb"
