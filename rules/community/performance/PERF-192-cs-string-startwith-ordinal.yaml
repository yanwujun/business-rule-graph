name: PERF-192-cs-string-startwith-ordinal
description: "Use StringComparison.Ordinal for perf-sensitive comparisons."
severity: info
type: ast_match
match:
  ast: "$S.StartsWith($PREFIX)"
  language: csharp
  file_glob: "**/*.cs"
