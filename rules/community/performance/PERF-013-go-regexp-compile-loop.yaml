name: go-regexp-compile-loop
description: "regexp.Compile inside loop recompiles on every iteration."
severity: warning
type: ast_match
match:
  ast: "regexp.Compile($PATTERN)"
  language: go
  file_glob: "**/*.go"
