name: PERF-71-py-global-import-in-func
description: "Import inside function body adds overhead per call."
severity: info
type: ast_match
match:
  ast: "def $FUNC($ARGS): import $MODULE"
  language: python
  file_glob: "**/*.py"
