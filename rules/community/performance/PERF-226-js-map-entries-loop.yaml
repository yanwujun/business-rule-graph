name: PERF-226-js-map-entries-loop
description: "Map.entries() creates iterator; cache if reused."
severity: info
type: ast_match
match:
  ast: "$MAP.entries()"
  language: javascript
  file_glob: "**/*.js"
