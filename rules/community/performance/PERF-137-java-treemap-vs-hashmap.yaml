name: PERF-137-java-treemap-vs-hashmap
description: "TreeMap has O(log n) operations; use HashMap unless order needed."
severity: info
type: ast_match
match:
  ast: "new TreeMap()"
  language: java
  file_glob: "**/*.java"
