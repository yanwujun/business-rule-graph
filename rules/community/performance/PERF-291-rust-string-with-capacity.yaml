name: PERF-291-rust-string-with-capacity
description: "String::new without capacity; use with_capacity for known sizes."
severity: info
type: ast_match
match:
  ast: "String::new()"
  language: rust
  file_glob: "**/*.rs"
