name: PERF-289-ts-worker-create-loop
description: "Worker creation in loop; use worker pool."
severity: warning
type: ast_match
match:
  ast: "new Worker($URL)"
  language: typescript
  file_glob: "**/*.ts"
