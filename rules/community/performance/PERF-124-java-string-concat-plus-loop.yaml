name: PERF-124-java-string-concat-plus-loop
description: "String concatenation in loop; use StringBuilder."
severity: warning
type: ast_match
match:
  ast: "$S = $S + $PART"
  language: java
  file_glob: "**/*.java"
