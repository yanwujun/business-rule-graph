name: ts-string-concat-loop
description: "String concatenation in loop is slow. Use array join."
severity: warning
type: ast_match
match:
  ast: "$STR += $PART"
  language: typescript
  file_glob: "**/*.ts"
