name: PERF-117-ts-object-spread-loop
description: "Object spread in loop creates copies; mutate directly."
severity: info
type: ast_match
match:
  ast: "{...$OBJ, $KEY: $VAL}"
  language: typescript
  file_glob: "**/*.ts"
