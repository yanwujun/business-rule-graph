name: PERF-105-js-json-stringify-loop
description: "JSON.stringify in loop is expensive; cache if possible."
severity: info
type: ast_match
match:
  ast: "JSON.stringify($OBJ)"
  language: javascript
  file_glob: "**/*.js"
