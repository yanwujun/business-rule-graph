name: PERF-138-java-string-format-loop
description: "String.format in loop; use StringBuilder or pre-format."
severity: info
type: ast_match
match:
  ast: "String.format($FMT, $ARGS)"
  language: java
  file_glob: "**/*.java"
