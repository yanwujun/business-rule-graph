name: ruby-activerecord-find-loop
description: "ActiveRecord find inside loop causes N+1 problem."
severity: warning
type: ast_match
match:
  ast: "$MODEL.find($ID)"
  language: ruby
  file_glob: "**/*.rb"
