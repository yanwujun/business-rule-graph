name: PERF-233-js-formdata-append-loop
description: "FormData.append in loop; batch if possible."
severity: info
type: ast_match
match:
  ast: "$FD.append($KEY, $VALUE)"
  language: javascript
  file_glob: "**/*.js"
