name: PERF-214-py-socket-connect-loop
description: "Socket connect in loop; reuse connections."
severity: warning
type: ast_match
match:
  ast: "$SOCK.connect($ADDR)"
  language: python
  file_glob: "**/*.py"
