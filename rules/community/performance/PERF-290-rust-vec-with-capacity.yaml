name: PERF-290-rust-vec-with-capacity
description: "Vec::new without capacity; use with_capacity for known sizes."
severity: info
type: ast_match
match:
  ast: "Vec::new()"
  language: rust
  file_glob: "**/*.rs"
