name: PERF-235-js-appendchild-loop
description: "appendChild in loop causes reflow; batch with Fragment."
severity: warning
type: ast_match
match:
  ast: "$PARENT.appendChild($CHILD)"
  language: javascript
  file_glob: "**/*.js"
