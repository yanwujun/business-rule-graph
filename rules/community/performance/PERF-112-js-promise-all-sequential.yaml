name: PERF-112-js-promise-all-sequential
description: "Sequential awaits; use Promise.all for parallel execution."
severity: info
type: ast_match
match:
  ast: "await $PROMISE1"
  language: javascript
  file_glob: "**/*.js"
