name: SEC-570-js-email-injection
description: "Verify email transport uses TLS."
severity: warning
type: ast_match
match:
  ast: "nodemailer.createTransport($OPTS)"
  language: javascript
  file_glob: "**/*.js"
