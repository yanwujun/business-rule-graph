name: SEC-435-ts-cmdi
description: "Command injection via concatenation in exec()."
severity: error
type: ast_match
match:
  ast: "child_process.exec($CMD + $INPUT)"
  language: typescript
  file_glob: "**/*.ts"
