name: "Security: js no insert adjacent html (typescript)"
description: "Disallow insertAdjacentHTML with untrusted HTML."
severity: warning
type: ast_match

match:
  ast: "$EL.insertAdjacentHTML($POS, $HTML)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
