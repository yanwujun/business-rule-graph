name: SEC-659-cs-session-fixation
description: "Verify session is regenerated after authentication."
severity: info
type: ast_match
match:
  ast: "HttpContext.Session.Set($KEY, $VALUE)"
  language: csharp
  file_glob: "**/*.cs"
