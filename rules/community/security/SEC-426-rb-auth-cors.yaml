name: SEC-426-rb-auth-cors
description: "Open redirect risk if user controls redirect target."
severity: warning
type: ast_match
match:
  ast: "redirect_to($INPUT)"
  language: ruby
  file_glob: "**/*.rb"
