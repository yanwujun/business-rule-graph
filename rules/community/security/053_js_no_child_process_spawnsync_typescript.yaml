name: "Security: js no child process spawnsync (typescript)"
description: "Review child_process.spawnSync invocation input."
severity: warning
type: ast_match

match:
  ast: "child_process.spawnSync($CMD)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
