name: "Security: js no vm runincontext (javascript)"
description: "Disallow vm.runInContext with untrusted code."
severity: warning
type: ast_match

match:
  ast: "vm.runInContext($CODE, $CTX)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
