name: SEC-685-java-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "$PASSWORD.length() < $MIN"
  language: java
  file_glob: "**/*.java"
