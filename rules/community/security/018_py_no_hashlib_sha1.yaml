name: "Security: py no hashlib sha1"
description: "Disallow SHA1 for security-sensitive hashing."
severity: warning
type: ast_match

match:
  ast: "hashlib.sha1($DATA)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
