name: SEC-689-cs-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "$PASSWORD.Length < $MIN"
  language: csharp
  file_glob: "**/*.cs"
