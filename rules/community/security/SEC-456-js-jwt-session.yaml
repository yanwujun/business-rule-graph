name: SEC-456-js-jwt-session
description: "jwt.decode() does not verify signature; use jwt.verify()."
severity: warning
type: ast_match
match:
  ast: "jwt.decode($TOKEN)"
  language: javascript
  file_glob: "**/*.js"
