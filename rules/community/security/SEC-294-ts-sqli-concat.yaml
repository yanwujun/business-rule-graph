name: SEC-294-ts-sqli-concat
description: "SQL injection via string concatenation in connection.query()."
severity: error
type: ast_match
match:
  ast: "connection.query($SQL + $INPUT)"
  language: typescript
  file_glob: "**/*.ts"
