name: SEC-421-js-auth-cors
description: "Open redirect risk if user controls redirect target."
severity: warning
type: ast_match
match:
  ast: "res.redirect($INPUT)"
  language: javascript
  file_glob: "**/*.js"
