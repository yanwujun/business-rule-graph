name: SEC-378-js-crypto-misuse
description: "Math.random() is not cryptographically secure."
severity: warning
type: ast_match
match:
  ast: "Math.random()"
  language: javascript
  file_glob: "**/*.js"
