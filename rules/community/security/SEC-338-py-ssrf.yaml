name: SEC-338-py-ssrf
description: "SSRF risk via user-controlled URL in requests.get()."
severity: error
type: ast_match
match:
  ast: "requests.get($URL + $INPUT)"
  language: python
  file_glob: "**/*.py"
