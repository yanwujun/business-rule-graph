name: "Security: py no marshal load"
description: "Disallow marshal.load on untrusted input."
severity: warning
type: ast_match

match:
  ast: "marshal.load($SRC)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
