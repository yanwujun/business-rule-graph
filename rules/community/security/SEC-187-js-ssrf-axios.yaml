name: js-ssrf-axios
description: "axios.get with user-supplied URL risks SSRF."
severity: error
type: ast_match
match:
  ast: "axios.get(req.body.$URL)"
  language: javascript
  file_glob: "**/*.{js,mjs}"
