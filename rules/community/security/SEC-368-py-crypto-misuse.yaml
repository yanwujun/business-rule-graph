name: SEC-368-py-crypto-misuse
description: "MD5 is cryptographically broken; use SHA-256+."
severity: warning
type: ast_match
match:
  ast: "hashlib.md5($DATA)"
  language: python
  file_glob: "**/*.py"
