name: "Security: js no axios post (typescript)"
description: "Review outbound axios.post() for SSRF risk."
severity: warning
type: ast_match

match:
  ast: "axios.post($URL)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
