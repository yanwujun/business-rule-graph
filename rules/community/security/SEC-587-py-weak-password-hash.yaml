name: SEC-587-py-weak-password-hash
description: "Plain SHA-256 for passwords is weak; use bcrypt/argon2."
severity: warning
type: ast_match
match:
  ast: "hashlib.sha256($DATA).hexdigest()"
  language: python
  file_glob: "**/*.py"
