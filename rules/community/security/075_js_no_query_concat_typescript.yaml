name: "Security: js no query concat (typescript)"
description: "Disallow SQL query concatenation in db.query()."
severity: warning
type: ast_match

match:
  ast: "db.query($SQL + $EXPR)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
