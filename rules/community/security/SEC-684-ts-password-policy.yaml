name: SEC-684-ts-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "$PASSWORD.length < $MIN"
  language: typescript
  file_glob: "**/*.ts"
