name: SEC-725-go-owasp-extra
description: "template.HTML marks input as safe; verify it is trusted."
severity: warning
type: ast_match
match:
  ast: "template.HTML($INPUT)"
  language: go
  file_glob: "**/*.go"
