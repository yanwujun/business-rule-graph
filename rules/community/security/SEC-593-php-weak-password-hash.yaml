name: SEC-593-php-weak-password-hash
description: "crypt() is low-level; use password_hash()."
severity: warning
type: ast_match
match:
  ast: "crypt($PASS, $SALT)"
  language: php
  file_glob: "**/*.php"
