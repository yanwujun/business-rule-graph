name: csharp-deser-binaryformatter
description: "BinaryFormatter.Deserialize risks insecure deserialization."
severity: error
type: ast_match
match:
  ast: "new BinaryFormatter().Deserialize($STREAM)"
  language: c_sharp
  file_glob: "**/*.cs"
