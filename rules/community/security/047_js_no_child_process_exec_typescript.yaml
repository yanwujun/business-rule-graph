name: "Security: js no child process exec (typescript)"
description: "Disallow child_process.exec with untrusted input."
severity: warning
type: ast_match

match:
  ast: "child_process.exec($CMD)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
