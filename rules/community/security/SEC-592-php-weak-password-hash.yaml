name: SEC-592-php-weak-password-hash
description: "Verify bcrypt cost factor is sufficient (>= 12)."
severity: warning
type: ast_match
match:
  ast: "password_hash($PASS, PASSWORD_BCRYPT)"
  language: php
  file_glob: "**/*.php"
