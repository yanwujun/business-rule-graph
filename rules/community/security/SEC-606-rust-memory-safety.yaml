name: SEC-606-rust-memory-safety
description: "Mutable null pointer; ensure safety invariants."
severity: warning
type: ast_match
match:
  ast: "std::ptr::null_mut()"
  language: rust
  file_glob: "**/*.rs"
