name: SEC-523-rust-dynamic-code
description: "Unsafe block bypasses Rust safety guarantees."
severity: warning
type: ast_match
match:
  ast: "unsafe { $BODY }"
  language: rust
  file_glob: "**/*.rs"
