name: SEC-554-ts-nosql-injection
description: "Verify MongoDB find query is not user-controlled."
severity: warning
type: ast_match
match:
  ast: "$DB.find($QUERY)"
  language: typescript
  file_glob: "**/*.ts"
