name: SEC-686-go-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "len($PASSWORD) < $MIN"
  language: go
  file_glob: "**/*.go"
