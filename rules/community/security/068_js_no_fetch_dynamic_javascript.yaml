name: "Security: js no fetch dynamic (javascript)"
description: "Review outbound fetch() for SSRF risk."
severity: warning
type: ast_match

match:
  ast: "fetch($URL)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
