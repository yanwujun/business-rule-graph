name: py-log-password
description: "Logging password value exposes credentials in log files."
severity: error
type: ast_match
match:
  ast: "logging.info($MSG, password)"
  language: python
  file_glob: "**/*.py"
