name: "Security: js no inner html assign (javascript)"
description: "Disallow direct innerHTML assignment."
severity: warning
type: ast_match

match:
  ast: "$EL.innerHTML = $HTML"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
