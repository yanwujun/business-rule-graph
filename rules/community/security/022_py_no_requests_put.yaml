name: "Security: py no requests put"
description: "Review outbound PUT requests for SSRF risk."
severity: warning
type: ast_match

match:
  ast: "requests.put($URL)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
