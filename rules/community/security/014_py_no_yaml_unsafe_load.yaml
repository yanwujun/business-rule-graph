name: "Security: py no yaml unsafe load"
description: "Disallow yaml.unsafe_load."
severity: warning
type: ast_match

match:
  ast: "yaml.unsafe_load($SRC)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
