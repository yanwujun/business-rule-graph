name: "Security: js no axios get (javascript)"
description: "Review outbound axios.get() for SSRF risk."
severity: warning
type: ast_match

match:
  ast: "axios.get($URL)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
