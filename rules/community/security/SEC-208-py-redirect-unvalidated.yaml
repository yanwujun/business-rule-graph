name: py-redirect-unvalidated
description: "Redirect to user-supplied URL risks open redirect."
severity: warning
type: ast_match
match:
  ast: "redirect(request.args[$KEY])"
  language: python
  file_glob: "**/*.py"
