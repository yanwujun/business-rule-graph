name: SEC-314-ts-xss
description: "XSS risk via insertAdjacentHTML()."
severity: error
type: ast_match
match:
  ast: "$EL.insertAdjacentHTML($POS, $INPUT)"
  language: typescript
  file_glob: "**/*.ts"
