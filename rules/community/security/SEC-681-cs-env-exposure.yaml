name: SEC-681-cs-env-exposure
description: "Verify env var is not logged with secrets."
severity: info
type: ast_match
match:
  ast: "Environment.GetEnvironmentVariable($KEY)"
  language: csharp
  file_glob: "**/*.cs"
