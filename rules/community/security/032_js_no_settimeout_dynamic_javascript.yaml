name: "Security: js no settimeout dynamic (javascript)"
description: "Review dynamic setTimeout usage."
severity: warning
type: ast_match

match:
  ast: "setTimeout($EXPR, $DELAY)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
