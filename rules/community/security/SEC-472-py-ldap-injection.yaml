name: SEC-472-py-ldap-injection
description: "LDAP injection via concatenated search filter."
severity: error
type: ast_match
match:
  ast: "ldap.search_s($BASE, $SCOPE, $FILTER + $INPUT)"
  language: python
  file_glob: "**/*.py"
