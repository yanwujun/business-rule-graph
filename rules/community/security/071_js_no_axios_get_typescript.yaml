name: "Security: js no axios get (typescript)"
description: "Review outbound axios.get() for SSRF risk."
severity: warning
type: ast_match

match:
  ast: "axios.get($URL)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
