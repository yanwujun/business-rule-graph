name: SEC-683-js-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "$PASSWORD.length < $MIN"
  language: javascript
  file_glob: "**/*.js"
