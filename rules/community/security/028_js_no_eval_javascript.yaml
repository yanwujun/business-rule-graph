name: "Security: js no eval (javascript)"
description: "Disallow eval() in JavaScript/TypeScript."
severity: warning
type: ast_match

match:
  ast: "eval($EXPR)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
