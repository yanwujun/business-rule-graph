name: SEC-474-cs-ldap-injection
description: "LDAP injection via concatenated DirectorySearcher."
severity: error
type: ast_match
match:
  ast: "DirectorySearcher($FILTER + $INPUT)"
  language: csharp
  file_glob: "**/*.cs"
