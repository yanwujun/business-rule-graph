name: SEC-586-py-weak-password-hash
description: "MD5 for password hashing is insecure; use bcrypt/argon2."
severity: warning
type: ast_match
match:
  ast: "hashlib.md5($DATA).hexdigest()"
  language: python
  file_glob: "**/*.py"
