name: js-helmet-missing
description: "Express app without helmet() lacks security headers."
severity: warning
type: ast_match
match:
  ast: "express()"
  language: javascript
  file_glob: "**/*.{js,mjs}"
