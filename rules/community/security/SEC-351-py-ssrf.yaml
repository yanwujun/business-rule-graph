name: SEC-351-py-ssrf
description: "SSRF risk via user-controlled URL in httpx.post()."
severity: error
type: ast_match
match:
  ast: "httpx.post($INPUT, $ARGS)"
  language: python
  file_glob: "**/*.py"
