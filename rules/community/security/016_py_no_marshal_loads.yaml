name: "Security: py no marshal loads"
description: "Disallow marshal.loads on untrusted input."
severity: warning
type: ast_match

match:
  ast: "marshal.loads($SRC)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
