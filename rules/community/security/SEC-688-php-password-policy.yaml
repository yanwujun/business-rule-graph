name: SEC-688-php-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "strlen($PASSWORD) < $MIN"
  language: php
  file_glob: "**/*.php"
