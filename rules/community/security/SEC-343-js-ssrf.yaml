name: SEC-343-js-ssrf
description: "SSRF risk via user-controlled URL in axios.get()."
severity: error
type: ast_match
match:
  ast: "axios.get($URL + $INPUT)"
  language: javascript
  file_glob: "**/*.js"
