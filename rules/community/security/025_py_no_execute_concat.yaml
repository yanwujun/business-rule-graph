name: "Security: py no execute concat"
description: "Disallow SQL query concatenation in execute()."
severity: warning
type: ast_match

match:
  ast: "cursor.execute($SQL + $EXPR)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
