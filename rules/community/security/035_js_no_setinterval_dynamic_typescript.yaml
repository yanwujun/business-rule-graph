name: "Security: js no setinterval dynamic (typescript)"
description: "Review dynamic setInterval usage."
severity: warning
type: ast_match

match:
  ast: "setInterval($EXPR, $DELAY)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
