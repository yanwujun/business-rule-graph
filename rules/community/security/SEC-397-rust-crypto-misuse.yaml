name: SEC-397-rust-crypto-misuse
description: "MD5 is cryptographically broken; use SHA-256+."
severity: warning
type: ast_match
match:
  ast: "md5::compute($DATA)"
  language: rust
  file_glob: "**/*.rs"
