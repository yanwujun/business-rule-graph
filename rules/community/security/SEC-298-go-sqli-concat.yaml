name: SEC-298-go-sqli-concat
description: "SQL injection via string concatenation in db.Query()."
severity: error
type: ast_match
match:
  ast: "db.Query($SQL + $INPUT)"
  language: go
  file_glob: "**/*.go"
