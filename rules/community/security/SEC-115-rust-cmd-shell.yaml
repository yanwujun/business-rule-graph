name: rust-cmd-shell
description: "Shell execution via std::process risks command injection."
severity: error
type: ast_match
match:
  ast: "Command::new(\"sh\").arg(\"-c\").arg($CMD)"
  language: rust
  file_glob: "**/*.rs"
