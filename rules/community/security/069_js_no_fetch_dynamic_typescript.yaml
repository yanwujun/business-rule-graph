name: "Security: js no fetch dynamic (typescript)"
description: "Review outbound fetch() for SSRF risk."
severity: warning
type: ast_match

match:
  ast: "fetch($URL)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
