name: "Security: js no vm runinnewcontext (javascript)"
description: "Disallow vm.runInNewContext with untrusted code."
severity: warning
type: ast_match

match:
  ast: "vm.runInNewContext($CODE)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
