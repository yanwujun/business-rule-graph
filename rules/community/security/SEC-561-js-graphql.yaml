name: SEC-561-js-graphql
description: "Verify GraphQL query is not user-controlled."
severity: warning
type: ast_match
match:
  ast: "graphql($SCHEMA, $QUERY)"
  language: javascript
  file_glob: "**/*.js"
