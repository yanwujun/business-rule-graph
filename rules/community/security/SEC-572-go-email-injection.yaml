name: SEC-572-go-email-injection
description: "Verify SMTP parameters are sanitized."
severity: warning
type: ast_match
match:
  ast: "smtp.SendMail($HOST, $AUTH, $FROM, $TO, $MSG)"
  language: go
  file_glob: "**/*.go"
