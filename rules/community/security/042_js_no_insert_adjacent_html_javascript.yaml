name: "Security: js no insert adjacent html (javascript)"
description: "Disallow insertAdjacentHTML with untrusted HTML."
severity: warning
type: ast_match

match:
  ast: "$EL.insertAdjacentHTML($POS, $HTML)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
