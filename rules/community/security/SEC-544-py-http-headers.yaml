name: SEC-544-py-http-headers
description: "HSTS disabled; enable with sufficient max-age."
severity: warning
type: ast_match
match:
  ast: "SECURE_HSTS_SECONDS = 0"
  language: python
  file_glob: "**/*.py"
