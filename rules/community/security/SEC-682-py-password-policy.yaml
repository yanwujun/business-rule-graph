name: SEC-682-py-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "len($PASSWORD) < $MIN"
  language: python
  file_glob: "**/*.py"
