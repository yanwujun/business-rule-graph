name: "Security: py no dill load"
description: "Disallow dill.load on potentially untrusted input."
severity: warning
type: ast_match

match:
  ast: "dill.load($SRC)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
