name: "Security: js no crypto createdecipher (typescript)"
description: "Disallow deprecated createDecipher API."
severity: warning
type: ast_match

match:
  ast: "crypto.createDecipher($ALG, $KEY)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
