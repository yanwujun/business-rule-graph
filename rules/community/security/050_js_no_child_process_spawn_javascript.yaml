name: "Security: js no child process spawn (javascript)"
description: "Review child_process.spawn invocation input."
severity: warning
type: ast_match

match:
  ast: "child_process.spawn($CMD)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
