name: go-path-open
description: "os.Open with unvalidated path risks path traversal."
severity: error
type: ast_match
match:
  ast: "os.Open(r.URL.Query().Get($PARAM))"
  language: go
  file_glob: "**/*.go"
