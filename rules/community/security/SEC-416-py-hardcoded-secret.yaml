name: SEC-416-py-hardcoded-secret
description: "Hardcoded private key; use secure key storage."
severity: warning
type: ast_match
match:
  ast: "PRIVATE_KEY = $VALUE"
  language: python
  file_glob: "**/*.py"
