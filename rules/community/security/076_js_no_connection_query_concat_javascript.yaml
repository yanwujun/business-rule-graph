name: "Security: js no connection query concat (javascript)"
description: "Disallow SQL query concatenation in connection.query()."
severity: warning
type: ast_match

match:
  ast: "connection.query($SQL + $EXPR)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
