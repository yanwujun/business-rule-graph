name: "Security: js no pool query concat (javascript)"
description: "Disallow SQL query concatenation in pool.query()."
severity: warning
type: ast_match

match:
  ast: "pool.query($SQL + $EXPR)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
