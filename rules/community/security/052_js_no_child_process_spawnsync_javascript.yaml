name: "Security: js no child process spawnsync (javascript)"
description: "Review child_process.spawnSync invocation input."
severity: warning
type: ast_match

match:
  ast: "child_process.spawnSync($CMD)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
