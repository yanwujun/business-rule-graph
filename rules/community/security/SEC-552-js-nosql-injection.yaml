name: SEC-552-js-nosql-injection
description: "NoSQL injection risk in findOne()."
severity: warning
type: ast_match
match:
  ast: "$COLLECTION.findOne($INPUT)"
  language: javascript
  file_glob: "**/*.js"
