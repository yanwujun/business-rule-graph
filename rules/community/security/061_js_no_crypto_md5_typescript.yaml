name: "Security: js no crypto md5 (typescript)"
description: "Disallow MD5 for security-sensitive hashing."
severity: warning
type: ast_match

match:
  ast: "crypto.createHash('md5')"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
