name: "Security: js no exec command (javascript)"
description: "Avoid deprecated execCommand where possible."
severity: warning
type: ast_match

match:
  ast: "document.execCommand($CMD)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
