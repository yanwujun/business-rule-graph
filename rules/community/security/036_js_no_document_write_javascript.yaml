name: "Security: js no document write (javascript)"
description: "Disallow document.write due XSS risk."
severity: warning
type: ast_match

match:
  ast: "document.write($HTML)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
