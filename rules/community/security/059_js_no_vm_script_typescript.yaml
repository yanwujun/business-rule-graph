name: "Security: js no vm script (typescript)"
description: "Review dynamic vm.Script compilation."
severity: warning
type: ast_match

match:
  ast: "new vm.Script($CODE)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
