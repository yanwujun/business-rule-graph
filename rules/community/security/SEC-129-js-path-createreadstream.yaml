name: js-path-createreadstream
description: "fs.createReadStream with user input risks path traversal."
severity: error
type: ast_match
match:
  ast: "fs.createReadStream(req.query.$PATH)"
  language: javascript
  file_glob: "**/*.{js,mjs}"
