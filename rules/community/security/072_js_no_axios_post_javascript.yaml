name: "Security: js no axios post (javascript)"
description: "Review outbound axios.post() for SSRF risk."
severity: warning
type: ast_match

match:
  ast: "axios.post($URL)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
