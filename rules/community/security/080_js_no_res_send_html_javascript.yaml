name: "Security: js no res send html (javascript)"
description: "Review direct HTML send for XSS risk."
severity: warning
type: ast_match

match:
  ast: "res.send($HTML)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
