name: "Security: js no crypto createcipher (javascript)"
description: "Disallow deprecated createCipher API."
severity: warning
type: ast_match

match:
  ast: "crypto.createCipher($ALG, $KEY)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
