name: SEC-403-py-hardcoded-secret
description: "Hardcoded AWS secret; use environment variables."
severity: warning
type: ast_match
match:
  ast: "AWS_SECRET_ACCESS_KEY = $VALUE"
  language: python
  file_glob: "**/*.py"
