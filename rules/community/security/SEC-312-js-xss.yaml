name: SEC-312-js-xss
description: "XSS risk via insertAdjacentHTML()."
severity: error
type: ast_match
match:
  ast: "$EL.insertAdjacentHTML($POS, $INPUT)"
  language: javascript
  file_glob: "**/*.js"
