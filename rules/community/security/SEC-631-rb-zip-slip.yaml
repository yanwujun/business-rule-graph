name: SEC-631-rb-zip-slip
description: "Zip slip risk; validate entry names before extraction."
severity: error
type: ast_match
match:
  ast: "Zip::File.open($PATH)"
  language: ruby
  file_glob: "**/*.rb"
