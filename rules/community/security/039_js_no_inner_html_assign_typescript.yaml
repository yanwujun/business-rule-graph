name: "Security: js no inner html assign (typescript)"
description: "Disallow direct innerHTML assignment."
severity: warning
type: ast_match

match:
  ast: "$EL.innerHTML = $HTML"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
