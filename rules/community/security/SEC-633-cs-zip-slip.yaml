name: SEC-633-cs-zip-slip
description: "Zip slip risk; validate paths."
severity: error
type: ast_match
match:
  ast: "ZipFile.ExtractToDirectory($PATH, $DEST)"
  language: csharp
  file_glob: "**/*.cs"
