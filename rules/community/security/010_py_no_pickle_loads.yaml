name: "Security: py no pickle loads"
description: "Disallow pickle.loads on potentially untrusted input."
severity: warning
type: ast_match

match:
  ast: "pickle.loads($SRC)"
  language: python
  file_glob: "**/*.py"
  max_matches: 100
