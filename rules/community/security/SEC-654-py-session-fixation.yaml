name: SEC-654-py-session-fixation
description: "Verify session is regenerated after authentication."
severity: info
type: ast_match
match:
  ast: "session[$KEY] = $VALUE"
  language: python
  file_glob: "**/*.py"
