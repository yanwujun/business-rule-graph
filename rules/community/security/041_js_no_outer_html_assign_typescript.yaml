name: "Security: js no outer html assign (typescript)"
description: "Disallow direct outerHTML assignment."
severity: warning
type: ast_match

match:
  ast: "$EL.outerHTML = $HTML"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
