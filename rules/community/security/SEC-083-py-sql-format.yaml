name: py-sql-format
description: "String format in SQL query risks SQL injection."
severity: error
type: ast_match
match:
  ast: "cursor.execute($QUERY.format($ARGS))"
  language: python
  file_glob: "**/*.py"
