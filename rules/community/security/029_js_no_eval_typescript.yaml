name: "Security: js no eval (typescript)"
description: "Disallow eval() in JavaScript/TypeScript."
severity: warning
type: ast_match

match:
  ast: "eval($EXPR)"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
