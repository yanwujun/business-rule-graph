name: "Security: js no crypto sha1 (typescript)"
description: "Disallow SHA1 for security-sensitive hashing."
severity: warning
type: ast_match

match:
  ast: "crypto.createHash('sha1')"
  language: typescript
  file_glob: "**/*.ts"
  max_matches: 100
