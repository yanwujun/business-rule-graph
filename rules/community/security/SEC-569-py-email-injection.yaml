name: SEC-569-py-email-injection
description: "Verify email parameters are sanitized."
severity: warning
type: ast_match
match:
  ast: "send_mail($SUBJECT, $BODY, $FROM, $TO)"
  language: python
  file_glob: "**/*.py"
