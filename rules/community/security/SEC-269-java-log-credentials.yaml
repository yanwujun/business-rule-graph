name: java-log-credentials
description: "Logging credential values exposes them in log files."
severity: error
type: ast_match
match:
  ast: "logger.info($MSG, password)"
  language: java
  file_glob: "**/*.java"
