name: SEC-687-rb-password-policy
description: "Verify minimum password length is at least 12."
severity: info
type: ast_match
match:
  ast: "$PASSWORD.length < $MIN"
  language: ruby
  file_glob: "**/*.rb"
