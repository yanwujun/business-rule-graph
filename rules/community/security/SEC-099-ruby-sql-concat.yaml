name: ruby-sql-concat
description: "String concatenation in SQL query risks SQL injection."
severity: error
type: ast_match
match:
  ast: "connection.execute($A + $B)"
  language: ruby
  file_glob: "**/*.rb"
