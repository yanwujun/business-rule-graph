name: "Security: js no child process exec (javascript)"
description: "Disallow child_process.exec with untrusted input."
severity: warning
type: ast_match

match:
  ast: "child_process.exec($CMD)"
  language: javascript
  file_glob: "**/*.js"
  max_matches: 100
