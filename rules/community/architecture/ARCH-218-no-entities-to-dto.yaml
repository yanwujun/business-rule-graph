name: "ARCH-218-no-entities-to-dto"
description: "Entities should not depend on DTOs."
severity: error
type: path_match
match:
  from:
    file_glob: "**/entities/**"
    kind: [function, method, class]
  to:
    file_glob: "**/dto/**"
    kind: [function, method, class]
  max_distance: 1
