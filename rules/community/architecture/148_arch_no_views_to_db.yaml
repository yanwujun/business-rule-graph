name: "Architecture: arch no views to db"
description: "Views should not access DB layer directly."
severity: warning
type: path_match

match:
  from:
    file_glob: "**/views/**"
    kind: [function, method, class]
  to:
    file_glob: "**/db/**"
    kind: [function, method, class]
  max_distance: 1
