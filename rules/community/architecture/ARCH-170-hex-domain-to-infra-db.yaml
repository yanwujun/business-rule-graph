name: hex-domain-to-infra-db
description: "Domain layer must not import infrastructure database modules."
severity: error
type: path_match
match:
  from:
    file_glob: "**/domain/**"
  to:
    file_glob: "**/infrastructure/database/**"
