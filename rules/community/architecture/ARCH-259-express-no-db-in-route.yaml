name: "ARCH-259-express-no-db-in-route"
description: "Express routes should not access database directly."
severity: warning
type: path_match
match:
  from:
    file_glob: "**/routes/**"
    kind: [function, method, class]
  to:
    file_glob: "**/db/**"
    kind: [function, method, class]
  max_distance: 1
