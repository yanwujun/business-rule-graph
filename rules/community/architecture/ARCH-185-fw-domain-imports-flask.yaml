name: fw-domain-imports-flask
description: "Domain layer must not import Flask framework."
severity: error
type: ast_match
match:
  ast: "from flask import $NAME"
  language: python
  file_glob: "**/domain/**/*.py"
