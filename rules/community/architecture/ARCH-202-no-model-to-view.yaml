name: "ARCH-202-no-model-to-view"
description: "Models should not depend on views."
severity: error
type: path_match
match:
  from:
    file_glob: "**/models/**"
    kind: [function, method, class]
  to:
    file_glob: "**/views/**"
    kind: [function, method, class]
  max_distance: 1
