name: "ARCH-213-no-domain-to-routes"
description: "Domain should not depend on routes."
severity: error
type: path_match
match:
  from:
    file_glob: "**/domain/**"
    kind: [function, method, class]
  to:
    file_glob: "**/routes/**"
    kind: [function, method, class]
  max_distance: 1
