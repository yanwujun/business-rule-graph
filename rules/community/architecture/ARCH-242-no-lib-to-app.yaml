name: "ARCH-242-no-lib-to-app"
description: "Library code should not depend on application code."
severity: error
type: path_match
match:
  from:
    file_glob: "**/lib/**"
    kind: [function, method, class]
  to:
    file_glob: "**/app/**"
    kind: [function, method, class]
  max_distance: 1
