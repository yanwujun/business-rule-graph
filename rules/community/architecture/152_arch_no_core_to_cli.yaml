name: "Architecture: arch no core to cli"
description: "Core should not depend on CLI layer."
severity: warning
type: path_match

match:
  from:
    file_glob: "**/core/**"
    kind: [function, method, class]
  to:
    file_glob: "**/cli/**"
    kind: [function, method, class]
  max_distance: 1
