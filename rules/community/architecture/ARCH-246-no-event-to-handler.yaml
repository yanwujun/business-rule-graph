name: "ARCH-246-no-event-to-handler"
description: "Events should not depend on handlers."
severity: warning
type: path_match
match:
  from:
    file_glob: "**/events/**"
    kind: [function, method, class]
  to:
    file_glob: "**/handlers/**"
    kind: [function, method, class]
  max_distance: 1
