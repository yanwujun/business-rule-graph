name: "Architecture: arch no domain to framework"
description: "Domain should not depend on framework layer."
severity: warning
type: path_match

match:
  from:
    file_glob: "**/domain/**"
    kind: [function, method, class]
  to:
    file_glob: "**/framework/**"
    kind: [function, method, class]
  max_distance: 1
