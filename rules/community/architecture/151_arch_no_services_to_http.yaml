name: "Architecture: arch no services to http"
description: "Services should stay transport-agnostic."
severity: warning
type: path_match

match:
  from:
    file_glob: "**/services/**"
    kind: [function, method, class]
  to:
    file_glob: "**/http/**"
    kind: [function, method, class]
  max_distance: 1
