name: "Architecture: arch no controller to db"
description: "Controllers should not call DB layer directly."
severity: warning
type: path_match

match:
  from:
    file_glob: "**/controllers/**"
    kind: [function, method, class]
  to:
    file_glob: "**/db/**"
    kind: [function, method, class]
  max_distance: 1
