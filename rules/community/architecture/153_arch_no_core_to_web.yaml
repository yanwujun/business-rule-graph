name: "Architecture: arch no core to web"
description: "Core should not depend on web layer."
severity: warning
type: path_match

match:
  from:
    file_glob: "**/core/**"
    kind: [function, method, class]
  to:
    file_glob: "**/web/**"
    kind: [function, method, class]
  max_distance: 1
